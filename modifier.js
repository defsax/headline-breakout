/*
var Modifier = (function() {
  var x = 0;
  var currentPower;
  var elapsedTime;
  var maxTime;
  return{
    setCurrentPower: function(modifier){
      currentPower = modifier;
      console.log(currentPower);
      currentPower();
      
    },
    update: function(dt){
      //if(
        console.log(dt);
    }
  };
})();
*/
